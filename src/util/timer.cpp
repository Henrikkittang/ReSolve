#include"timer.hpp"

/*Timer::Timer()
    : m_time( std::chrono::high_resolution_clock::now() )
{}


double Timer::milliseconds() const
{
    auto now = std::chrono::high_resolution_clock::now();
    return std::chrono::duration<double, std::milli>(now - m_time).count();
}

double Timer::seconds() const
{
    auto now = std::chrono::high_resolution_clock::now();
    return std::chrono::duration<double>(now - m_time).count();
}


void Timer::reset()
{
    m_time = std::chrono::high_resolution_clock::now();
}*/


